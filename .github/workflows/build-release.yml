name: Build and Release

on:
  push:
    tags:
      - 'v*'  # Trigger on version tags like v1.3.1

permissions:
  contents: write  # Global permissions for all jobs

env:
  XRAY_FALLBACK_VERSION: 'v25.12.8'  # Fallback version if API fails

jobs:
  build-mac-arm64:
    runs-on: macos-latest  # Apple Silicon runner
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Get latest Xray version
        id: xray_version
        run: |
          # Try to get latest version, fallback if API fails
          RESPONSE=$(curl -sL "https://api.github.com/repos/XTLS/Xray-core/releases/latest")
          XRAY_VERSION=$(echo "$RESPONSE" | python3 -c "import sys, json; data=json.load(sys.stdin); print(data.get('tag_name', ''))" 2>/dev/null || echo "")
          if [ -z "$XRAY_VERSION" ]; then
            echo "API failed, using fallback version: $XRAY_FALLBACK_VERSION"
            XRAY_VERSION="${{ env.XRAY_FALLBACK_VERSION }}"
          fi
          echo "version=$XRAY_VERSION" >> $GITHUB_OUTPUT
          echo "Using Xray version: $XRAY_VERSION"

      - name: Download Xray for macOS ARM64
        run: |
          XRAY_VERSION="${{ steps.xray_version.outputs.version }}"
          echo "Downloading Xray version: $XRAY_VERSION"
          mkdir -p resources/bin/darwin-arm64
          curl -sL -o xray.zip "https://github.com/XTLS/Xray-core/releases/download/${XRAY_VERSION}/Xray-macos-arm64-v8a.zip"
          unzip -o xray.zip -d resources/bin/darwin-arm64/
          rm -f xray.zip
          # Move shared files to common bin directory
          mv resources/bin/darwin-arm64/geoip.dat resources/bin/ 2>/dev/null || true
          mv resources/bin/darwin-arm64/geosite.dat resources/bin/ 2>/dev/null || true
          rm -f resources/bin/darwin-arm64/LICENSE resources/bin/darwin-arm64/README.md
          chmod +x resources/bin/darwin-arm64/xray

      - name: Download Chrome
        run: |
          npx @puppeteer/browsers install chrome@129.0.6668.58 --path resources/puppeteer

      - name: Build macOS ARM64
        run: npm run build:mac
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mac-arm64
          path: dist/*.dmg

  build-mac-x64:
    runs-on: macos-15-intel  # Intel Mac runner (updated from deprecated macos-13)
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Get latest Xray version
        id: xray_version
        run: |
          # Try to get latest version, fallback if API fails
          RESPONSE=$(curl -sL "https://api.github.com/repos/XTLS/Xray-core/releases/latest")
          XRAY_VERSION=$(echo "$RESPONSE" | python3 -c "import sys, json; data=json.load(sys.stdin); print(data.get('tag_name', ''))" 2>/dev/null || echo "")
          if [ -z "$XRAY_VERSION" ]; then
            echo "API failed, using fallback version: $XRAY_FALLBACK_VERSION"
            XRAY_VERSION="${{ env.XRAY_FALLBACK_VERSION }}"
          fi
          echo "version=$XRAY_VERSION" >> $GITHUB_OUTPUT
          echo "Using Xray version: $XRAY_VERSION"

      - name: Download Xray for macOS x64
        run: |
          XRAY_VERSION="${{ steps.xray_version.outputs.version }}"
          echo "Downloading Xray version: $XRAY_VERSION"
          mkdir -p resources/bin/darwin-x64
          curl -sL -o xray.zip "https://github.com/XTLS/Xray-core/releases/download/${XRAY_VERSION}/Xray-macos-64.zip"
          unzip -o xray.zip -d resources/bin/darwin-x64/
          rm -f xray.zip
          # Move shared files to common bin directory
          mv resources/bin/darwin-x64/geoip.dat resources/bin/ 2>/dev/null || true
          mv resources/bin/darwin-x64/geosite.dat resources/bin/ 2>/dev/null || true
          rm -f resources/bin/darwin-x64/LICENSE resources/bin/darwin-x64/README.md
          chmod +x resources/bin/darwin-x64/xray

      - name: Download Chrome
        run: |
          npx @puppeteer/browsers install chrome@129.0.6668.58 --path resources/puppeteer

      - name: Build macOS x64
        run: npm run build:mac
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mac-x64
          path: dist/*.dmg

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Get latest Xray version
        id: xray_version
        shell: bash
        run: |
          # Try to get latest version, fallback if API fails
          RESPONSE=$(curl -sL "https://api.github.com/repos/XTLS/Xray-core/releases/latest")
          XRAY_VERSION=$(echo "$RESPONSE" | python3 -c "import sys, json; data=json.load(sys.stdin); print(data.get('tag_name', ''))" 2>/dev/null || echo "")
          if [ -z "$XRAY_VERSION" ]; then
            echo "API failed, using fallback version: $XRAY_FALLBACK_VERSION"
            XRAY_VERSION="${{ env.XRAY_FALLBACK_VERSION }}"
          fi
          echo "version=$XRAY_VERSION" >> $GITHUB_OUTPUT
          echo "Using Xray version: $XRAY_VERSION"

      - name: Download Xray for Windows
        shell: bash
        run: |
          XRAY_VERSION="${{ steps.xray_version.outputs.version }}"
          echo "Downloading Xray version: $XRAY_VERSION"
          mkdir -p resources/bin/win32-x64
          curl -sL -o xray.zip "https://github.com/XTLS/Xray-core/releases/download/${XRAY_VERSION}/Xray-windows-64.zip"
          unzip -o xray.zip -d resources/bin/win32-x64/
          rm -f xray.zip
          # Move shared files to common bin directory
          mv resources/bin/win32-x64/geoip.dat resources/bin/ 2>/dev/null || true
          mv resources/bin/win32-x64/geosite.dat resources/bin/ 2>/dev/null || true
          rm -f resources/bin/win32-x64/LICENSE resources/bin/win32-x64/README.md

      - name: Download Chrome
        run: |
          npx @puppeteer/browsers install chrome@129.0.6668.58 --path resources/puppeteer

      - name: Build Windows
        run: npm run build:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows
          path: |
            dist/*.exe
            dist/win-unpacked

  release:
    needs: [build-mac-arm64, build-mac-x64, build-windows]
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/**/*.dmg
            artifacts/**/*.exe
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
